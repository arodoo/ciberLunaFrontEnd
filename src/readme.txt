This application is being created to manage cell phones in a company who fix them. Services are in a 
sub-folder an components in other one. inside the components we have add, to list, login and navbar. 
Each of those folders has their name's logic. The paths are in the file "app.routes.ts" and the JS and 
CSS library used is Bootstrap's library, which is imported in "angular.json".